<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/root"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- RecyclerView -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerListing"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="gone" />

    <!-- Shimmer Container showing multiple shimmer items -->
    <LinearLayout
        android:id="@+id/shimmerLayoutContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:visibility="visible"
        android:padding="8dp">

        <!-- Include shimmer placeholder multiple times for nice effect -->
        <include layout="@layout/shimmer_movie_item" />
        <include layout="@layout/shimmer_movie_item" />
        <include layout="@layout/shimmer_movie_item" />
        <include layout="@layout/shimmer_movie_item" />
        <include layout="@layout/shimmer_movie_item" />
    </LinearLayout>

</FrameLayout>
